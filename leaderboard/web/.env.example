# Copy to .env.local for local dev, or set these in Vercel → Project → Settings → Environment Variables.
# Both are read server-side only; they are never exposed to the browser.

TURSO_DATABASE_URL=libsql://f405-leaderboard-yourname.turso.io
TURSO_AUTH_TOKEN=your-turso-db-token
